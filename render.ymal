services:
  - type: web
    name: heart-disease-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run heart_web.py --server.port=$PORT --server.address=0.0.0.0"
